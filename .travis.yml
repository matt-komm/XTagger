sudo: required
language: cpp
compiler:
  - gcc
jobs:
  include:
    - stage: EnvCPU, Unpacker and Training check
      script:
        - travis_wait 45 Tests/Env/testSetupEnv.sh
        - travis_wait 10 Tests/Ops/buildOps.sh
notifications:
  email:
    recipients:
        - vilius.cepaitis@cern.ch
        - mkomm@cern.ch
    on_success: change
    on_failure: always


